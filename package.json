{
  "name": "reminder-service",
  "version": "1.0.0",
  "description": "Reminder is a simple node/typescript service with a websocket interface to handle clients.",
  "main": "main.js",
  "scripts": {
    "build": "tsc -w",
    "dev:run": "nodemon build/main.js",
    "dev": "concurrently npm:build npm:dev:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Samuel Sorial",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.3.0",
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.13",
    "prettier": "^2.4.1"
  },
  "dependencies": {
    "@types/ws": "^8.2.0",
    "ws": "^8.2.3"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 4,
    "semi": true,
    "printWidth": 80
  }
}
